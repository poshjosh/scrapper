All Keywords returned should have the database name already set

override CapturerContext#getKeywords();

look for all literals "" and make them properties
com.loosedb
Remove all ViewManager etc from CapturerApp

CapturerConfigFactory
public Set<String> deleteOrphanSyncPairs() 

before inserting extracted records 
search for all foreign keys
replace each value with matching key
if no value found, replacing with default key if avialable ???

MappingsMgr was changed to MappingsConfig, stuff were removed






DefaultFormatter
protected Map<String, Object> formatIntegerColumns(String, Map)

Override SearchSite.getType()

